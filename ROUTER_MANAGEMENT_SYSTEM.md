# 📡 Система учёта роутеров

**Дата:** 07.11.2025  
**Версия:** 3.0

## 🎯 Обзор

Реализована полная система учёта роутеров для сотрудников с автоматическим списанием при создании подключений.

---

## 📋 Основной функционал

### 1. Управление роутерами сотрудников

**Путь:** Управление сотрудниками → Управление роутерами

#### Добавление роутеров
- Выбор сотрудника
- Ввод названия роутера (например: "Keenetic Giga")
- Ввод количества (целое число)
- Если роутер уже есть у сотрудника → количество суммируется

#### Списание роутеров
- Выбор сотрудника
- Выбор роутера из списка
- Списывается 1 штука
- Если остаток = 0 → запись удаляется

### 2. Выбор роутера при создании подключения

**Шаг 4/8: Модель роутера**

#### Варианты выбора:
1. **Выбор из списка** - показываются все роутеры, зарегистрированные в системе
2. **Ввод вручную** - если нужного роутера нет в списке

#### Логика отображения:
- Если в системе есть роутеры → показывается список + кнопка "Ввести вручную"
- Если роутеров нет → сразу предлагается ввод вручную

### 3. Автоматическое списание роутера

После выбора исполнителей система проверяет наличие роутера:

#### Сценарий 1: Ни у кого нет роутера
→ Подключение создаётся без списания роутера

#### Сценарий 2: Только у одного есть роутер
→ Роутер автоматически списывается с этого сотрудника

#### Сценарий 3: У обоих есть роутер
→ Система предлагает выбрать, с кого списать

---

## 🔄 Логика списания

### Аналогично материалам:

```
1. Если у одного исполнителя есть роутер, у другого нет
   → Списывается с того, у кого есть

2. Если у обоих есть роутер
   → Пользователь выбирает плательщика

3. Если ни у кого нет роутера
   → Подключение создаётся без списания
```

### Важно:
- Роутер **не обязателен** для создания подключения
- Списывается всегда **1 штука**
- В отчёте указывается, с кого списан роутер

---

## 📊 База данных

### Новая таблица: `employee_routers`

```sql
CREATE TABLE employee_routers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    employee_id INTEGER NOT NULL,
    router_name TEXT NOT NULL,
    quantity INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (employee_id) REFERENCES employees(id) ON DELETE CASCADE
)
```

### Новые методы в `database.py`:

1. **`add_router_to_employee(employee_id, router_name, quantity)`**
   - Добавляет роутеры сотруднику
   - Если роутер уже есть → суммирует количество

2. **`deduct_router_from_employee(employee_id, router_name, quantity)`**
   - Списывает роутеры у сотрудника
   - Проверяет наличие достаточного количества
   - Если остаток = 0 → удаляет запись

3. **`get_employee_routers(employee_id)`**
   - Возвращает список всех роутеров сотрудника

4. **`get_router_quantity(employee_id, router_name)`**
   - Возвращает количество конкретного роутера

5. **`get_all_router_names()`**
   - Возвращает список всех уникальных названий роутеров в системе

---

## 🎨 Интерфейс

### Меню "Управление роутерами"

```
📡 Управление роутерами

Выберите сотрудника:
[📡 Иванов (5 шт.)]
[📡 Петров (нет)]
[◀️ Назад]
```

### Карточка сотрудника

```
📡 Роутеры сотрудника

👤 Иванов Иван И.

📊 Текущие роутеры:
  • Keenetic Giga: 3 шт.
  • TP-Link Archer: 2 шт.

Выберите действие:
[➕ Добавить роутеры]
[➖ Списать роутер]
[◀️ Назад]
```

### Выбор роутера при подключении

```
✅ Адрес: ул. Ленина, 10

🌐 Шаг 4/8: Модель роутера

Выберите роутер из списка или введите вручную:
[📡 Keenetic Giga]
[📡 TP-Link Archer]
[📡 Zyxel Keenetic]
[✏️ Ввести вручную]
```

### Выбор плательщика роутера

```
📡 Выбор плательщика роутера

Роутер: Keenetic Giga

У нескольких исполнителей есть этот роутер.
Выберите, с кого списать роутер:
[📡 Иванов (3 шт.)]
[📡 Петров (2 шт.)]
```

### Подтверждение с роутером

```
📋 Подтверждение данных

🏢 Тип: МКД
📍 Адрес: ул. Ленина, 10
🌐 Роутер: Keenetic Giga
🔌 Порт: 12

📏 Метраж:
  • ВОЛС: 100 м
  • Витая пара: 50 м

👥 Исполнители (2):
  • Иванов
  • Петров

Метраж на каждого (для зарплаты):
  • ВОЛС: 50 м
  • Витая пара: 25 м

💰 Материалы списываются с: Иванов
📡 Роутер списывается с: Иванов

📸 Фото: 5 шт.

Всё верно? Подтвердите создание отчета.
[✅ Подтвердить] [❌ Отменить]
```

---

## 🔧 Технические изменения

### `config.py`
Добавлены новые состояния:
- `SELECT_ROUTER` (3) - выбор роутера при создании подключения
- `SELECT_ROUTER_PAYER` (9) - выбор плательщика роутера
- `SELECT_EMPLOYEE_FOR_ROUTER` (18) - выбор сотрудника для управления роутерами
- `SELECT_ROUTER_ACTION` (19) - выбор действия с роутерами
- `ENTER_ROUTER_NAME` (20) - ввод названия роутера
- `ENTER_ROUTER_QUANTITY` (21) - ввод количества роутеров

### `database.py`
- Создана таблица `employee_routers`
- Добавлены 5 новых методов для работы с роутерами

### `handlers/employees.py`
Добавлены функции:
- `select_employee_for_router()` - выбор сотрудника
- `select_router_action()` - выбор действия (добавить/списать)
- `enter_router_name()` - ввод названия или выбор для списания
- `enter_router_quantity()` - ввод количества

### `handlers/connection.py`
Изменены функции:
- `enter_address()` - теперь показывает список роутеров
- `select_router()` - новая функция для выбора роутера
- `check_routers_and_proceed()` - проверка наличия роутеров
- `select_router_payer()` - выбор плательщика роутера
- `show_confirmation()` - показывает информацию о роутере
- `confirm_connection()` - списывает роутер после создания

Обновлён `ConversationHandler`:
- Заменено состояние `ENTER_ROUTER` на `SELECT_ROUTER`
- Добавлено состояние `SELECT_ROUTER_PAYER`

### `bot.py`
- Импортированы новые функции из `handlers/employees`
- Импортированы новые состояния из `config`
- Созданы wrapper-функции для передачи `db`
- Обновлён `manage_conv` с новыми состояниями

---

## 📖 Примеры использования

### Пример 1: Добавление роутеров

```
1. Управление сотрудниками → Управление роутерами
2. Выбрать сотрудника
3. Нажать "➕ Добавить роутеры"
4. Ввести название: "Keenetic Giga"
5. Ввести количество: 5

Результат:
✅ Роутеры добавлены!
👤 Сотрудник: Иванов
📡 Роутер: Keenetic Giga
➕ Добавлено: 5 шт.
📊 Всего: 5 шт.
```

### Пример 2: Создание подключения с роутером

```
1. Новое подключение
2. Выбрать тип, загрузить фото, ввести адрес
3. Шаг 4/8: Выбрать "Keenetic Giga" из списка
4. Ввести порт, метраж
5. Выбрать исполнителей: Иванов, Петров

Система проверяет:
- У Иванова есть Keenetic Giga (5 шт.)
- У Петрова нет

Результат:
→ Роутер автоматически списывается с Иванова
→ В подтверждении: "Роутер списывается с: Иванов"
```

### Пример 3: Выбор плательщика

```
Ситуация:
- Роутер: TP-Link Archer
- Иванов: 3 шт.
- Петров: 2 шт.

Система показывает:
📡 Выбор плательщика роутера
[📡 Иванов (3 шт.)]
[📡 Петров (2 шт.)]

Пользователь выбирает Иванова
→ Роутер списывается с Иванова
```

### Пример 4: Ввод вручную

```
1. Шаг 4/8: Модель роутера
2. Нажать "✏️ Ввести вручную"
3. Ввести: "Новая модель X100"

Результат:
→ Роутер сохраняется в подключении
→ Не списывается ни с кого (т.к. его нет в системе)
```

---

## ⚠️ Важные моменты

### 1. Роутер не обязателен
- Подключение можно создать без роутера в системе
- Можно ввести любое название вручную

### 2. Списание всегда 1 штука
- При создании подключения списывается ровно 1 роутер
- Независимо от количества исполнителей

### 3. Уникальность по названию
- Роутеры различаются по названию
- Регистр учитывается: "Keenetic Giga" ≠ "keenetic giga"

### 4. Автоматическое удаление
- Если количество становится 0 → запись удаляется
- Это нормально и экономит место в БД

### 5. Список роутеров
- Формируется из всех уникальных названий в системе
- Обновляется автоматически при добавлении новых

---

## 🧪 Тестирование

### Тест 1: Добавление роутеров

```bash
1. Добавить роутер "Keenetic Giga" (5 шт.) → Иванову
2. Проверить список роутеров Иванова
3. Добавить ещё "Keenetic Giga" (3 шт.) → Иванову
4. Проверить: должно быть 8 шт.
```

### Тест 2: Списание роутера

```bash
1. Создать подключение с роутером "Keenetic Giga"
2. Выбрать исполнителей: Иванов (есть), Петров (нет)
3. Проверить: роутер должен списаться с Иванова
4. Проверить баланс Иванова: должно быть 7 шт.
```

### Тест 3: Выбор плательщика

```bash
1. Добавить "TP-Link Archer" → Иванову (3 шт.)
2. Добавить "TP-Link Archer" → Петрову (2 шт.)
3. Создать подключение с "TP-Link Archer"
4. Выбрать обоих исполнителей
5. Проверить: должен показаться выбор плательщика
```

### Тест 4: Ввод вручную

```bash
1. Создать подключение
2. На шаге роутера нажать "Ввести вручную"
3. Ввести "Новая модель"
4. Проверить: подключение должно создаться
5. Проверить: роутер не должен списаться
```

---

## 🎯 Преимущества

✅ **Гибкость** - можно использовать роутеры из системы или вводить вручную  
✅ **Автоматизация** - автоматическое списание при создании подключения  
✅ **Контроль** - полный учёт роутеров каждого сотрудника  
✅ **Прозрачность** - видно, с кого списан роутер  
✅ **Удобство** - быстрый выбор из списка

---

## 🤖 Статус

- ✅ Реализовано
- ✅ Протестировано
- ✅ Бот работает (PID: 50351)
- ✅ Готово к использованию

---

**Версия:** 3.0  
**Дата обновления:** 07.11.2025


