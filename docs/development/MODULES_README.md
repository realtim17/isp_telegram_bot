# Модульная структура бота

## 📁 Структура проекта

```
isp_telegram_bot/
├── bot.py                    # Главный файл, точка входа
├── config.py                 # Конфигурация и константы
├── database.py               # Работа с базой данных
├── report_generator.py       # Генерация Excel-отчетов
│
├── handlers/                 # Обработчики команд и событий
│   ├── __init__.py
│   ├── commands.py          # Базовые команды (/start, /help, /cancel)
│   ├── connection.py        # Создание подключений
│   ├── reports.py           # Формирование отчетов
│   └── employees.py         # Управление сотрудниками
│
└── utils/                    # Вспомогательные модули
    ├── __init__.py
    ├── keyboards.py         # Клавиатуры для Telegram
    └── helpers.py           # Вспомогательные функции
```

## 📦 Описание модулей

### `config.py`
**Назначение:** Центральная конфигурация бота
- Загрузка переменных окружения из `.env`
- Определение констант состояний для ConversationHandler
- Настройка логирования
- Проверка прав администратора

**Основные компоненты:**
- `TELEGRAM_BOT_TOKEN` - токен бота
- `ADMIN_IDS` - список ID администраторов
- `REPORTS_CHANNEL_ID` - ID канала для отчетов
- `is_admin(user_id)` - проверка прав администратора
- Константы состояний: `UPLOAD_PHOTOS`, `ENTER_ADDRESS`, и т.д.

---

### `handlers/commands.py`
**Назначение:** Базовые команды бота
- `/start` - приветствие и инструкции
- `/help` - справка по использованию
- `/cancel` - отмена текущей операции

**Функции:**
- `start_command()` - обработчик /start
- `help_command()` - обработчик /help
- `cancel_command()` - отмена операции
- `cancel_and_start_new()` - отмена и переход к новому действию

---

### `handlers/connection.py`
**Назначение:** Создание отчетов о подключении

**Основной процесс (7 шагов):**
1. **Загрузка фотографий** - до 10 фото с места подключения
2. **Адрес** - адрес подключения абонента
3. **Роутер** - модель роутера
4. **Порт** - номер порта
5. **ВОЛС** - метраж волоконно-оптической линии
6. **Витая пара** - метраж витой пары
7. **Исполнители** - выбор сотрудников

**Функции:**
- `new_connection_start()` - начало процесса
- `upload_photos()` - загрузка фотографий
- `ask_address()` - запрос адреса
- `enter_address()` - сохранение адреса
- `enter_router()` - сохранение модели роутера
- `enter_port()` - сохранение порта
- `enter_fiber()` - сохранение метража ВОЛС
- `enter_twisted()` - сохранение метража витой пары
- `select_employee_toggle()` - выбор сотрудников
- `show_confirmation()` - подтверждение данных
- `confirm_connection()` - сохранение в БД и отправка отчета

**Особенности:**
- Автоматический расчет метража на каждого исполнителя
- Валидация вводимых данных
- Отправка отчета пользователю и в канал (если настроен)

---

### `handlers/reports.py`
**Назначение:** Формирование сводных отчетов

**Процесс:**
1. Выбор сотрудника
2. Выбор периода (неделя/месяц/все время)
3. Генерация и отправка Excel-файла

**Функции:**
- `report_start()` - выбор сотрудника
- `report_select_period()` - выбор периода
- `report_generate()` - генерация отчета

**Периоды отчетов:**
- Последняя неделя (7 дней)
- Последний месяц (30 дней)
- Все время

---

### `handlers/employees.py`
**Назначение:** Управление сотрудниками (только для администраторов)

**Функции:**
- `manage_employees_start()` - главное меню управления
- `manage_action()` - обработка выбора действия
- `add_employee_name()` - добавление сотрудника
- `delete_employee_confirm()` - удаление сотрудника

**Доступные действия:**
- ➕ Добавить сотрудника
- ➖ Удалить сотрудника
- 📋 Список сотрудников

**Права доступа:**
- Только пользователи из списка `ADMIN_IDS` в `.env`

---

### `utils/keyboards.py`
**Назначение:** Создание клавиатур для Telegram

**Функции:**
- `get_main_keyboard()` - главная клавиатура

**Кнопки главной клавиатуры:**
- 📝 Новое подключение
- 📊 Сводный отчет
- 👥 Управление сотрудниками
- ℹ️ Помощь

---

### `utils/helpers.py`
**Назначение:** Вспомогательные функции

**Функции:**
- `send_connection_report()` - отправка красиво отформатированного отчета

**Возможности:**
- Отправка отчета с фотографиями (альбом до 10 фото)
- Автоматический расчет метража на каждого исполнителя
- Отправка отчета пользователю
- Отправка отчета в канал (если настроен `REPORTS_CHANNEL_ID`)

---

### `bot.py`
**Назначение:** Главный файл, точка входа

**Основные компоненты:**
- Импорт всех модулей
- Инициализация приложения Telegram
- Создание ConversationHandler для каждого процесса:
  - `connection_conv` - создание подключений
  - `report_conv` - формирование отчетов
  - `manage_conv` - управление сотрудниками
- Регистрация обработчиков
- Запуск polling

**Обертки для передачи БД:**
Функции-обертки передают объект `db` в обработчики:
- `enter_twisted_wrapper()`
- `select_employee_toggle_wrapper()`
- `confirm_connection_wrapper()`
- и т.д.

---

## 🔧 Добавление нового функционала

### Пример 1: Добавление новой команды

1. **Создайте обработчик** в `handlers/commands.py`:
```python
async def new_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Описание новой команды"""
    await update.message.reply_text("Новая команда работает!")
```

2. **Зарегистрируйте обработчик** в `bot.py`:
```python
application.add_handler(CommandHandler('new_cmd', new_command))
```

### Пример 2: Добавление новой клавиатуры

1. **Создайте функцию** в `utils/keyboards.py`:
```python
def get_custom_keyboard() -> ReplyKeyboardMarkup:
    keyboard = [
        [KeyboardButton("Кнопка 1")],
        [KeyboardButton("Кнопка 2")]
    ]
    return ReplyKeyboardMarkup(keyboard, resize_keyboard=True)
```

2. **Используйте в обработчиках**:
```python
from utils.keyboards import get_custom_keyboard

await update.message.reply_text(
    "Выберите действие:",
    reply_markup=get_custom_keyboard()
)
```

### Пример 3: Добавление нового ConversationHandler

1. **Создайте модуль** `handlers/new_feature.py`
2. **Определите состояния** в `config.py`
3. **Создайте обработчики** в новом модуле
4. **Зарегистрируйте ConversationHandler** в `bot.py`

---

## ✅ Преимущества модульной структуры

1. **Читаемость** - каждый модуль отвечает за свою задачу
2. **Поддержка** - легко найти и исправить код
3. **Масштабируемость** - просто добавлять новый функционал
4. **Тестирование** - модули можно тестировать независимо
5. **Повторное использование** - функции можно использовать в разных местах
6. **Совместная разработка** - разные разработчики могут работать над разными модулями

---

## 🚀 Запуск

```bash
# Активация виртуального окружения
source .venv311/bin/activate

# Запуск бота
python bot.py
```

---

## 📝 Конфигурация

Все настройки хранятся в файле `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token
ADMIN_USER_IDS=123456789,987654321
REPORTS_CHANNEL_ID=-1001234567890
```

---

## 🔍 Отладка

Логи сохраняются в файл `bot.log` и выводятся в консоль.

Уровень логирования настраивается в `config.py`:
```python
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO  # Измените на DEBUG для детальных логов
)
```

---

## 📚 Дополнительная документация

- `README.md` - общая информация о проекте
- `ARCHITECTURE.md` - архитектура системы
- `CHANGELOG.md` - история изменений
- `QUICKSTART.md` - быстрый старт

