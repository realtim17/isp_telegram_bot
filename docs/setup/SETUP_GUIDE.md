# 🚀 Руководство по установке и настройке бота

## Быстрый старт

### 1. Установка зависимостей

```bash
cd /Users/tim_not/isp_telegram_bot
source .venv311/bin/activate
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните обязательные параметры:

```env
# Токен бота от @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ID администраторов (через запятую)
ADMIN_USER_IDS=123456789,987654321

# ID канала для отчётов (опционально)
REPORTS_CHANNEL_ID=-1001234567890
```

### 3. Запуск бота

**Обычный запуск:**
```bash
python bot.py
```

**Запуск в фоновом режиме:**
```bash
nohup python bot.py >> bot.log 2>&1 &
```

**Или используйте скрипт:**
```bash
./run.sh
```

### 4. Проверка работы

```bash
# Проверить, что бот запущен
ps aux | grep "python bot.py" | grep -v grep

# Посмотреть логи
tail -f bot.log
```

---

## Настройка канала для отчётов

### Шаг 1: Создание канала

1. Откройте Telegram
2. Создайте новый канал
3. Сделайте канал приватным
4. Добавьте бота в канал как администратора

### Шаг 2: Получение ID канала

**Способ 1: Через веб-версию**

1. Откройте канал в веб-версии Telegram
2. ID канала будет в URL: `https://web.telegram.org/k/#-1001234567890`
3. Скопируйте число с минусом: `-1001234567890`

**Способ 2: Через бота**

1. Отправьте сообщение в канал
2. Перешлите это сообщение боту @userinfobot
3. Он покажет ID канала

### Шаг 3: Добавление в .env

```env
REPORTS_CHANNEL_ID=-1001234567890
```

### Шаг 4: Перезапуск бота

```bash
pkill -9 -f "python.*bot\.py"
python bot.py
```

---

## Получение токена бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "ISP Report Bot")
   - Введите username (должен заканчиваться на "bot", например: "isp_report_bot")
4. Скопируйте полученный токен
5. Добавьте в `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

---

## Получение ID пользователя

### Способ 1: Через @userinfobot

1. Найдите бота @userinfobot в Telegram
2. Отправьте ему любое сообщение
3. Он ответит вашим ID

### Способ 2: Через логи бота

1. Запустите бота
2. Отправьте боту команду `/start`
3. Посмотрите в `bot.log` - там будет ваш ID

---

## Управление ботом

### Запуск

```bash
cd /Users/tim_not/isp_telegram_bot
source .venv311/bin/activate
python bot.py
```

### Остановка

```bash
pkill -9 -f "python.*bot\.py"
```

### Перезапуск

```bash
./RESTART_BOT.sh
```

Или вручную:

```bash
pkill -9 -f "python.*bot\.py"
sleep 2
source .venv311/bin/activate
nohup python bot.py >> bot.log 2>&1 &
```

### Просмотр логов

```bash
# Последние 50 строк
tail -50 bot.log

# Следить в реальном времени
tail -f bot.log

# Поиск ошибок
grep -i error bot.log
```

---

## Автозапуск через systemd (Linux)

1. Скопируйте файл сервиса:
   ```bash
   sudo cp isp_bot.service /etc/systemd/system/
   ```

2. Отредактируйте пути в файле:
   ```bash
   sudo nano /etc/systemd/system/isp_bot.service
   ```

3. Активируйте сервис:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable isp_bot
   sudo systemctl start isp_bot
   ```

4. Проверьте статус:
   ```bash
   sudo systemctl status isp_bot
   ```

---

## Автозапуск через Docker

```bash
docker-compose up -d
```

Остановка:
```bash
docker-compose down
```

Логи:
```bash
docker-compose logs -f
```

---

## Решение проблем

### Бот не запускается

1. Проверьте токен в `.env`
2. Проверьте, что виртуальное окружение активировано
3. Проверьте логи: `tail -50 bot.log`

### Бот не отвечает

1. Проверьте, что процесс запущен: `ps aux | grep bot.py`
2. Проверьте логи на ошибки: `grep -i error bot.log`
3. Перезапустите бота

### Ошибка "Conflict: terminated by other getUpdates"

Запущено несколько экземпляров бота. Убейте все процессы:

```bash
pkill -9 -f "python.*bot\.py"
```

Затем запустите заново.

### База данных повреждена

Создайте резервную копию и пересоздайте:

```bash
cp isp_bot.db isp_bot.db.backup
rm isp_bot.db
python bot.py  # БД создастся автоматически
```

---

## Следующие шаги

После успешной установки:

1. 📖 Изучите [документацию по разработке](../development/ARCHITECTURE.md)
2. 🔧 Настройте сотрудников через команду `/manage_employees`
3. 📝 Создайте тестовое подключение
4. 📊 Проверьте генерацию отчётов

---

## Полезные ссылки

- [Архитектура проекта](../development/ARCHITECTURE.md)
- [История изменений](../history/CHANGELOG.md)
- [Примеры использования](../development/EXAMPLES.md)


