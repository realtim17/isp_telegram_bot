# Обновление системы загрузки фотографий

## Дата: 06.11.2025

## Что изменилось

Система загрузки фотографий была полностью переработана. Теперь вместо загрузки всех фото одним блоком, пользователь проходит **7 последовательных шагов**, каждый из которых соответствует определённой категории фотографий.

## Новые категории фотографий

### Обязательные категории:

1. **📍 Маршрут линии**
   - Фотографии маршрута прокладки линии
   - Пропустить нельзя

2. **🔌 ОРК (разварка) / Коммутатор (порт)**
   - Фото ОРК или коммутатора с номером порта
   - Пропустить нельзя

3. **🏠 Место входа в помещение**
   - Фото места сверления/входа кабеля в помещение
   - Пропустить нельзя

5. **📡 Место установки WiFi роутера / оконечивание**
   - Фото установленного роутера и оконечивания
   - Пропустить нельзя

### Опциональные категории:

4. **📦 Внутренняя укладка в помещении**
   - Фото прокладки кабеля внутри помещения
   - Можно пропустить

6. **⚡ Замер скорости**
   - Скриншот замера скорости интернета
   - Можно пропустить через кнопку "Пропустить" или "Нет WiFi роутера"

7. **🔑 Доступ на роутер**
   - Фото с данными доступа к роутеру
   - Можно пропустить через кнопку "Пропустить" или "Нет WiFi роутера"

## Технические изменения

### 1. Конфигурация (`config.py`)

**Добавлены новые константы состояний:**
```python
PHOTO_ROUTE = 1           # Маршрут линии
PHOTO_ORK = 2            # ОРК/Коммутатор
PHOTO_ENTRY = 3          # Место входа в помещение
PHOTO_INDOOR = 4         # Внутренняя укладка (опционально)
PHOTO_ROUTER = 5         # Место установки роутера
PHOTO_SPEED = 6          # Замер скорости (опционально)
PHOTO_ACCESS = 7         # Доступ на роутер (опционально)
```

**Добавлен словарь категорий:**
```python
PHOTO_CATEGORIES = {
    'route': {...},
    'ork': {...},
    'entry': {...},
    'indoor': {...},
    'router': {...},
    'speed': {...},
    'access': {...}
}
```

### 2. База данных (`database.py`)

**Изменена структура таблицы `connection_photos`:**
- Добавлено поле `photo_category` для хранения категории фото
- Метод `create_connection` теперь принимает `photos_by_category` вместо `photo_file_ids`

```sql
ALTER TABLE connection_photos ADD COLUMN photo_category TEXT NOT NULL DEFAULT 'other'
```

### 3. Обработчики подключений (`handlers/connection.py`)

**Новые функции:**
- `upload_photo_category()` - универсальный обработчик загрузки фото для любой категории
- `handle_photo_navigation()` - обработка кнопок "Продолжить", "Пропустить", "Нет WiFi роутера"
- `proceed_to_address()` - переход к вводу адреса после всех фото

**ConversationHandler обновлён:**
- 7 новых состояний для каждой категории фото
- Каждое состояние обрабатывает загрузку фото и навигацию
- Проверка обязательности загрузки для каждой категории

### 4. Утилиты (`utils/helpers.py`)

**Обновлена функция `send_connection_report()`:**
- Теперь принимает `photos_by_category` вместо списка фото
- Собирает все фото из всех категорий в один альбом для отправки

### 5. Главный файл (`bot.py`)

- Импортируется готовый `connection_conv` из `handlers/connection.py`
- Убраны старые обёртки для обработчиков подключений

## Новый процесс создания подключения

### Шаги (всего 14):

1. **Тип подключения** - выбор МКД/ЧС/Юр
2-8. **Загрузка фотографий по категориям** (7 шагов)
9. **Адрес подключения**
10. **Модель роутера**
11. **Номер порта**
12. **Метраж ВОЛС**
13. **Метраж витой пары**
14. **Выбор исполнителей**

### Особенности:

- Для опциональных категорий есть кнопка "⏭ Пропустить"
- Для категорий "Замер скорости" и "Доступ на роутер" есть специальная кнопка "❌ Нет WiFi роутера", которая пропускает обе категории
- Обязательные категории нельзя пропустить - при попытке нажать "Продолжить" без фото, система выдаст предупреждение
- Можно загружать несколько фото в каждую категорию

## Обратная совместимость

- Старые записи в БД автоматически получат категорию 'other'
- Миграция выполняется автоматически при первом запуске обновлённой версии
- Все существующие подключения продолжают работать без изменений

## Инструкция для исполнителей

1. Запустите создание нового подключения через кнопку "📝 Новое подключение"
2. Выберите тип подключения
3. Последовательно загружайте фото для каждой категории:
   - Система подскажет, какие фото нужны на каждом шаге
   - Обязательные категории отмечены 🔴
   - Опциональные категории отмечены 🟢
4. После загрузки фото для категории нажмите "➡️ Продолжить"
5. Если категория опциональна, можно нажать "⏭ Пропустить"
6. Для категорий связанных с WiFi роутером есть кнопка "❌ Нет WiFi роутера"
7. После всех фото заполните остальные данные как обычно

## Преимущества новой системы

✅ **Структурированность** - каждое фото имеет чёткое назначение  
✅ **Понятность** - система подсказывает, что нужно сфотографировать  
✅ **Гибкость** - можно пропустить необязательные категории  
✅ **Качество** - исполнители не забудут важные фото  
✅ **Удобство** - специальная кнопка для случаев без WiFi роутера

